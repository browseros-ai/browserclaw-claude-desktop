paths:
  .github/workflows/release.yml:
    ignore:
      # actionlint v1.7.12 lacks queue support: https://github.com/rhysd/actionlint/pull/670
      - 'unexpected key "queue" for "concurrency" section'
